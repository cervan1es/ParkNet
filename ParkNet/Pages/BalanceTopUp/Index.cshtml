@page
@model PARKNET.Pages.BalanceTopUp.IndexModel

@{
    ViewData["Title"] = "Balance - Transactions";
}

<h1>Balance Top Up - Transactions History</h1>

<p>
    <a asp-page="Create">Top Up</a>
</p>
<label asp-for="Balance">Balance: </label>
<label>@Model.Balance</label>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Transaction[0].Value)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Transaction[0].Date)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Transaction) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Value)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                <a asp-page="./Details" asp-route-id="@item.TransactionID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.TransactionID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
